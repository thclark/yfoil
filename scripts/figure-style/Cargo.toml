[package]
name = "figure-style"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false
description = "Publication figure conventions shared by the validation scripts: physical size, font, math-style labels, PDF export"

[lints]
workspace = true

[dependencies]
plotters = "0.3"

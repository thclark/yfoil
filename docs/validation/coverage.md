# Branch coverage of the translated XFOIL subroutines

Generated by `cargo xtask coverage` on 2026-09-10. Do not edit; regenerate.

**Instrument.** The pristine XFOIL 6.99 double-precision reference (build patch series only, no instrumentation dumps) compiled and linked with `-fprofile-arcs -ftest-coverage` (`scripts/xfoil-build.sh --gcov`, `target/xfoil-ref/gcov/`), run on every case below from its tracked `xfoil.inp`/`panels.dat`, counters read back with gcov's JSON output. A *branch* is one outgoing edge of a conditional as the compiler emitted it: an `IF` contributes two edges, a `.AND.`/`.OR.` chain one pair per operand, a `DO` loop its zero-trip entry pair, and a loop whose back-edge test is attributed to its last statement adds a pair there. Which arm an edge is (fallthrough vs jump) is the compiler's choice and is reported as such, not as true/false; the *other edges* column gives the counts of the remaining edges on the same line, so the dead arm can be read off against the source.

**Toolchain.** gfortran/gcc 14.2.0, `gcov-14`. **Subroutine set.** `xtask/fixtures-config/coverage.toml` (77 subroutines in 10 files).

**Cases run (22).** `naca0012_n60_a2_re1e6`, `naca0012_n60_polar_re1e6`, `naca0012_n60_cl03_re1e6`, `naca0012_n60_a2_re1e6_type2`, `naca0012_n60_sharp_a2_re1e6`, `naca0012_n60_a2_re1e6_m03`, `naca0012_n60_a12_re1e6`, `naca0012_n60_a4_re1e5`, `naca0012_n60_a2_re1e6_xtr03`, `naca0012_n60_a2_repeat_re1e6`, `naca0012_n60_a2_cl03_re1e6`, `naca0012_n60_a2_re1e6_type3`, `naca0012_n60_a2_re1e6_m03_type2`, `naca0012_n60_a2_re1e6_xtr_coinc`, `naca0012_n60_a2_re1e6_damp`, `pangen_naca0012_n160`, `pangen_naca0012_n81`, `pangen_naca4412_n160`, `pangen_naca4412_n81`, `pangen_naca23012_n160`, `naca0012_n160_polar_up22_re1e6_iter100`, `naca4412_n160_polar_down16_re1e6_iter100`

## Completeness (CLAUDE.md Rule 6)

The fixture set is complete when every reachable branch in the translated subroutines has been taken at least once. Never-taken branches fall into three classes:

- **open** — never taken, no annotation. These are the objective for further cases and for the parameter/fuzz harness. "Numerically exact" is not yet claimable over them.
- **loop entry** — the zero-trip edge of a `DO` loop whose body always ran (or a loop inside an unreached block). Listed separately because they are structurally impossible on the analysis path (`DO IBL=2,NBL(IS)` with `NBL ≥ 2`). A `DO` loop that is an iteration cap (a Newton loop that could be exhausted) carries a reach note and stays in the open list.
- **unreachable** — annotated in `xtask/fixtures-config/coverage.toml` with a class and the reason: *structural* (impossible by construction on the analysis path), *mode* (a feature outside it: inverse design, image airfoil, flap hinge, interactive prompts), *guard* (array-bound or illegal-input STOP; yFoil has no fixed dimensions), *compiler* (no source-level decision).

Open branches with a known way to reach them carry a *reach* note; those are the next cases to add, and the rest are what the parameter/fuzz harness is for.

| | count |
|---|---|
| branches in the translated set | 1320 |
| taken | 1135 |
| never taken — open | **80** |
| never taken — loop entry | 21 |
| never taken — annotated unreachable | 84 |
| &nbsp;&nbsp;&nbsp;&nbsp;structural | 23 |
| &nbsp;&nbsp;&nbsp;&nbsp;mode | 35 |
| &nbsp;&nbsp;&nbsp;&nbsp;guard | 17 |
| &nbsp;&nbsp;&nbsp;&nbsp;compiler | 9 |
| subroutines never called | 3 (0 unexplained) |

**Verdict: incomplete — the open branches below are not yet covered by any case.**

Never called:

- `xpanel.f:UECALC` — never called anywhere in XFOIL 6.99 (dead code, translated for completeness)
- `xpanel.f:DSSET` — never called anywhere in XFOIL 6.99 (dead code, translated for completeness)
- `xblsys.f:DIT` — its only call site is commented out (xblsys.f:944)

## Per subroutine

| file | subroutine | calls | branches | taken | open | loop entry | unreachable |
|---|---|---:|---:|---:|---:|---:|---:|
| xpanel.f | APCALC | 27 | 6 | 5 | 0 | 0 | 1 |
| xpanel.f | NCALC | 27 | 12 | 7 | 0 | 0 | 5 |
| xpanel.f | PSILIN | 9738 | 68 | 36 | 0 | 3 | 29 |
| xpanel.f | PSWLIN | 16299 | 20 | 17 | 0 | 0 | 3 |
| xpanel.f | GGCALC | 17 | 20 | 20 | 0 | 0 | 0 |
| xpanel.f | QWCALC | 107 | 2 | 2 | 0 | 0 | 0 |
| xpanel.f | QDCALC | 105 | 42 | 42 | 0 | 0 | 0 |
| xpanel.f | XYWAKE | 105 | 6 | 5 | 0 | 0 | 1 |
| xpanel.f | STFIND | 1243 | 12 | 8 | **3** | 1 | 0 |
| xpanel.f | IBLPAN | 267 | 10 | 9 | 0 | 0 | 1 |
| xpanel.f | XICALC | 1243 | 14 | 14 | 0 | 0 | 0 |
| xpanel.f | UICALC | 367 | 4 | 4 | 0 | 0 | 0 |
| xpanel.f | UECALC | 0 | 4 | 0 | 0 | 4 | 0 |
| xpanel.f | QVFUE | 1226 | 4 | 4 | 0 | 0 | 0 |
| xpanel.f | QISET | 226 | 2 | 2 | 0 | 0 | 0 |
| xpanel.f | GAMQV | 1226 | 2 | 2 | 0 | 0 | 0 |
| xpanel.f | STMOVE | 1224 | 26 | 25 | **1** | 0 | 0 |
| xpanel.f | UESET | 1224 | 8 | 8 | 0 | 0 | 0 |
| xpanel.f | DSSET | 0 | 4 | 0 | 0 | 4 | 0 |
| xbl.f | SETBL | 1224 | 82 | 80 | **1** | 0 | 1 |
| xbl.f | IBLSYS | 267 | 6 | 5 | 0 | 0 | 1 |
| xbl.f | MRCHUE | 19 | 104 | 91 | **13** | 0 | 0 |
| xbl.f | MRCHDU | 1224 | 110 | 106 | **4** | 0 | 0 |
| xbl.f | XIFSET | 4934 | 8 | 7 | **1** | 0 | 0 |
| xbl.f | UPDATE | 1224 | 86 | 82 | **4** | 0 | 0 |
| xbl.f | DSLIM | 860238 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | TRCHEK | 206275 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | AXSET | 727420 | 6 | 6 | 0 | 0 | 0 |
| xblsys.f | TRCHEK2 | 206275 | 44 | 42 | **2** | 0 | 0 |
| xblsys.f | BLSYS | 856408 | 26 | 26 | 0 | 0 | 0 |
| xblsys.f | TESYS | 3942 | 4 | 4 | 0 | 0 | 0 |
| xblsys.f | BLPRV | 1069025 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | BLKIN | 1307936 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | BLVAR | 903676 | 42 | 41 | **1** | 0 | 0 |
| xblsys.f | BLMID | 886769 | 8 | 8 | 0 | 0 | 0 |
| xblsys.f | TRDIF | 12965 | 12 | 12 | 0 | 0 | 0 |
| xblsys.f | BLDIF | 869373 | 18 | 18 | 0 | 0 | 0 |
| xblsys.f | DAMPL | 1450692 | 4 | 4 | 0 | 0 | 0 |
| xblsys.f | DAMPL2 | 4148 | 10 | 9 | **1** | 0 | 0 |
| xblsys.f | HKIN | 2176287 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | DIL | 784994 | 2 | 2 | 0 | 0 | 0 |
| xblsys.f | DILW | 118682 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | HSL | 421956 | 2 | 2 | 0 | 0 | 0 |
| xblsys.f | CFL | 1557023 | 2 | 2 | 0 | 0 | 0 |
| xblsys.f | DIT | 0 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | HST | 600402 | 6 | 6 | 0 | 0 | 0 |
| xblsys.f | CFT | 1432195 | 0 | 0 | 0 | 0 | 0 |
| xblsys.f | HCT | 903676 | 0 | 0 | 0 | 0 | 0 |
| xoper.f | SPECAL | 105 | 22 | 16 | **3** | 0 | 3 |
| xoper.f | SPECCL | 2 | 16 | 12 | **2** | 0 | 2 |
| xoper.f | VISCAL | 107 | 46 | 41 | **1** | 0 | 4 |
| xsolve.f | GAUSS | 1295362 | 24 | 24 | 0 | 0 | 0 |
| xsolve.f | LUDCMP | 17 | 26 | 25 | 0 | 0 | 1 |
| xsolve.f | BAKSUB | 3453 | 14 | 14 | 0 | 0 | 0 |
| xsolve.f | BLSOLV | 1224 | 52 | 52 | 0 | 0 | 0 |
| xfoil.f | MRCL | 2903 | 22 | 14 | **2** | 0 | 6 |
| xfoil.f | COMSET | 2931 | 2 | 2 | 0 | 0 | 0 |
| xfoil.f | CPCALC | 571 | 6 | 4 | **2** | 0 | 0 |
| xfoil.f | CLCALC | 1674 | 4 | 4 | 0 | 0 | 0 |
| xfoil.f | CDCALC | 1243 | 8 | 7 | **1** | 0 | 0 |
| xfoil.f | NACA | 5 | 14 | 10 | 0 | 0 | 4 |
| xfoil.f | PANGEN | 10 | 102 | 64 | **26** | 8 | 4 |
| xfoil.f | TECALC | 134 | 2 | 2 | 0 | 0 | 0 |
| xutils.f | SETEXP | 105 | 12 | 8 | **2** | 0 | 2 |
| naca.f | NACA4 | 4 | 14 | 12 | 0 | 0 | 2 |
| naca.f | NACA5 | 1 | 20 | 13 | 0 | 0 | 7 |
| spline.f | SPLIND | 270 | 18 | 8 | **5** | 0 | 5 |
| spline.f | TRISOL | 332 | 4 | 4 | 0 | 0 | 0 |
| spline.f | SEVAL | 72254 | 4 | 4 | 0 | 0 | 0 |
| spline.f | DEVAL | 44738 | 4 | 4 | 0 | 0 | 0 |
| spline.f | D2VAL | 288 | 4 | 4 | 0 | 0 | 0 |
| spline.f | CURV | 2552 | 4 | 4 | 0 | 0 | 0 |
| spline.f | SINVRT | 44 | 4 | 3 | **1** | 0 | 0 |
| spline.f | SCALC | 59 | 2 | 2 | 0 | 0 | 0 |
| spline.f | SEGSPL | 182 | 8 | 5 | **1** | 0 | 2 |
| xgeom.f | LEFIND | 81 | 10 | 7 | **2** | 1 | 0 |
| xgeom.f | GEOPAR | 22 | 4 | 3 | **1** | 0 | 0 |

## Open branches (never taken, not annotated)

### `xpanel.f` — STFIND

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1365 | 1 (jump) | 90670, 1243, 89427 | `IF(GAM(I).GE.0.0 .AND. GAM(I+1).LT.0.0) GO TO 11` | GAM(1) < 0 at the upper TE node (negative alpha with a marginally negative TE vorticity); otherwise the scan starts where GAM >= 0 and stops at the first sign change |
| 1385 | 0 (fallthrough) | 1243 | `IF(SST .LE. S(I)  ) SST = S(I)   + 1.0E-7` | the stagnation point falling bitwise on a node (GAM(I) = 0 exactly): threshold-straddling territory |
| 1386 | 0 (fallthrough) | 1243 | `IF(SST .GE. S(I+1)) SST = S(I+1) - 1.0E-7` | the stagnation point falling bitwise on a node (GAM(I) = 0 exactly): threshold-straddling territory |

### `xpanel.f` — STMOVE

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1737 | 0 (fallthrough) | 44736 | `IF(UEDG(IBL,IS).LE.UEPS) THEN` | UEDG <= UEPS at a BL station after the stagnation-point shift: strongly separated case |

### `xbl.f` — SETBL

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 227 | 2 (fallthrough) | 2448, 202536, 2448 | `IF(IBL.EQ.ITRAN(IS) .AND. .NOT.TRAN) THEN` | IBL = ITRAN with TRAN false: a side that stays laminar to the TE (ITRAN past IBLTE), low Re |

### `xbl.f` — MRCHUE

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 628 | 0 (fallthrough) | 290 | `IF(CTI.LE.0.0) THEN` | CTI <= 0 at the transition station on the first march: a trip where the amplification is still 0 (XTR very near the LE) |
| 785 | 1 (jump) | 6 | `IF(DMAX .LE. 0.1) GO TO 109` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 788 | 1 (jump) | 6 | `IF(IBL.GT.3) THEN` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 789 | 1 (jump) | 6 | `IF(IBL.LE.IBLTE(IS)) THEN` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 792 | 0 (fallthrough) | 0 | `ELSE IF(IBL.EQ.IBLTE(IS)+1) THEN` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 792 | 1 (jump) | 0 | `ELSE IF(IBL.EQ.IBLTE(IS)+1) THEN` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 801 | 0 (fallthrough) | 6 | `IF(IBL.EQ.ITRAN(IS)) CTI = 0.05` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 805 | 1 (jump) | 6, 4, 2 | `IF(IBL.GT.2 .AND. IBL.LT.NBL(IS))` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 813 | 1 (jump) | 6, 3, 3 | `IF((.NOT.SIMI) .AND. (.NOT.TURB)) THEN` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 816 | 0 (fallthrough) | 3 | `IF(     TRAN) ITRAN(IS) = IBL` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 817 | 1 (jump) | 3 | `IF(.NOT.TRAN) ITRAN(IS) = IBL+2` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 823 | 0 (fallthrough) | 6 | `IF(WAKE) CALL BLVAR(3)` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |
| 827 | 0 (fallthrough) | 6 | `IF(WAKE) CALL BLMID(3)` | MRCHUE Newton failure with residual > 0.1 (the garbage-solution extrapolation); the 6 failures so far all had DMAX <= 0.1 |

### `xbl.f` — MRCHDU

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 940 | 0 (fallthrough) | 113606 | `IF(CTI.LE.0.0) CTI = 0.03` | stored CTAU <= 0 at a turbulent station (an UPDATE step driving CTAU negative) |
| 1111 | 1 (jump) | 1854 | `IF(IBL.GT.3) THEN` | MRCHDU Newton failure with residual > 0.1 (the extrapolation fallback); the 70 failures so far all had DMAX <= 0.1 |
| 1116 | 0 (fallthrough) | 33 | `ELSE IF(IBL.EQ.IBLTE(IS)+1) THEN` | MRCHDU Newton failure with residual > 0.1 (the extrapolation fallback); the 70 failures so far all had DMAX <= 0.1 |
| 1135 | 1 (jump) | 1936, 21, 1915 | `IF((.NOT.SIMI) .AND. (.NOT.TURB)) THEN` | MRCHDU Newton failure with residual > 0.1 (the extrapolation fallback); the 70 failures so far all had DMAX <= 0.1 |

### `xbl.f` — XIFSET

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1241 | 0 (fallthrough) | 44 | `IF(XIFORC .LT. 0.0) THEN` | trip upstream of the stagnation point (e.g. XTR 1 0.001 at positive alpha) |

### `xbl.f` — UPDATE

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1396 | 0 (fallthrough) | 12 | `IF(RLX*DAC .GT. DALMAX) RLX = DALMAX/DAC` | fixed-CL mode with the alpha step limited to +-0.5 deg: a CL target far from the current alpha (e.g. CL 1.0 from alpha 0) |
| 1397 | 0 (fallthrough) | 12 | `IF(RLX*DAC .LT. DALMIN) RLX = DALMIN/DAC` | fixed-CL mode with the alpha step limited to +-0.5 deg: a CL target far from the current alpha (e.g. CL 1.0 from alpha 0) |
| 1474 | 0 (fallthrough) | 204984 | `IF(RDN4 .LT. DLO) RLX = DLO/DN4` | an UPDATE step reducing a BL variable by more than 50 % (DLO) |
| 1537 | 2 (fallthrough) | 175543, 249, 175543 | `IF(UEDG(IBL-1,IS) .GT. 0.0 .AND.` | an island of negative Ue after UPDATE: strongly separated or diverging case |

### `xblsys.f` — TRCHEK2

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 390 | 0 (fallthrough) | 225946 | `IF(AX .LE. 0.0) GO TO 101` | AX <= 0 at a transition check (no amplification between the two stations) |
| 432 | 1 (jump) | 206275, 203740, 2535 | `TRFORC = XIFORC.GT.X1 .AND. XIFORC.LE.X2` | a laminar station with XIFORC <= X1: the trip moved upstream past a still-laminar station by STMOVE between iterations |

### `xblsys.f` — BLVAR

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 843 | 2 (fallthrough) | 118682, 784994, 118682 | `IF(ITYP.EQ.3 .AND. US2.GT.0.99995) THEN` | Us > 0.99995 in the wake: deep separation |

### `xblsys.f` — DAMPL2

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 2258 | 0 (fallthrough) | 117 | `IF(AX2 .LT. 0.0) THEN` | DAMP with a separated laminar profile (Hk > 3.5) at a log10(Rtheta) below GR0(Hk): the modified rate AX2 negative and clamped to 0 (low-Re laminar bubble with DAMP) |

### `xoper.f` — SPECAL

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 2767 | 1 (jump) | 213 | `DO 100 ITCL=1, 20` | iteration cap: SPECAL's 20-iteration CL(M) loop exhausted (MATYP 2 at M > 0 failing to settle) |
| 2776 | 1 (jump) | 213 | `DO 90 IRLX=1, 12` | iteration cap: SPECAL's 12-step RLX halving exhausted (MATYP 2 at M > 0) |
| 2807 | 1 (jump) | 105 | `IF(LVISC) THEN` | inviscid-only run (no VISC) |

### `xoper.f` — SPECCL

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 2846 | 1 (jump) | 6 | `DO 100 ITAL=1, 20` | iteration cap: SPECCL's 20-iteration alpha Newton exhausted (a CL target that cannot be met) |
| 2874 | 1 (jump) | 2 | `IF(LVISC) THEN` | inviscid-only run (no VISC) |

### `xoper.f` — VISCAL

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 2945 | 1 (jump) | 2 | `IF(LVISC) THEN` | inviscid-only run (no VISC), re-entered at the same alpha |

### `xfoil.f` — MRCL

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 843 | 0 (fallthrough) | 2903 | `IF(MINF .GE. 0.99) THEN` | TYPE 2 with M > 0 at a CL small enough that M/sqrt(CL) >= 0.99 |
| 854 | 0 (fallthrough) | 2881 | `IF(RRAT .GT. 100.0) THEN` | TYPE 2/3 at CL ~ 0 (Re ratio > 100), e.g. a symmetric section at alpha 0 with TYPE 2 |

### `xfoil.f` — CPCALC

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1075 | 0 (fallthrough) | 82842 | `IF(DEN .LE. 0.0) DENNEG = .TRUE.` | Karman-Tsien denominator <= 0: locally supersonic speed (high alpha at M >= 0.5) |
| 1078 | 0 (fallthrough) | 571 | `IF(DENNEG) THEN` | Karman-Tsien denominator <= 0: locally supersonic speed (high alpha at M >= 0.5) |

### `xfoil.f` — CDCALC

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1178 | 1 (jump) | 1243, 1225, 18 | `IF(LVISC .AND. LBLINI) THEN` | inviscid-only run (no VISC) |

### `xfoil.f` — PANGEN

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 1680 | 2 (fallthrough) | 4, 2436, 4 | `IF(SBLE.EQ.SB(I) .AND. SBLE.EQ.SB(I+1)) THEN` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0): LOAD of such a file, then PPAR |
| 1708 | 0 (fallthrough) | 10 | `IF(IBLE.NE.0) CVAVG = 10.0` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0): LOAD of such a file, then PPAR |
| 1738 | 0 (fallthrough) | 2430 | `IF(DSM.EQ.0.0 .OR. DSP.EQ.0.0) THEN` | a buffer airfoil with a corner (doubled node), e.g. a flapped section: LOAD then PPAR |
| 1755 | 3 (jump) | 1706, 4, 1706, 0, 1706 | `IF(SB(I).EQ.SBLE .OR. I.EQ.IBLE .OR. I.EQ.IBLE+1) THEN` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0) |
| 1755 | 4 (fallthrough) | 1706, 4, 1706, 0, 1706 | `IF(SB(I).EQ.SBLE .OR. I.EQ.IBLE .OR. I.EQ.IBLE+1) THEN` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0) |
| 1794 | 0 (fallthrough) | 1216, 0, 0 | `IF(XOC.GT.XSREF1 .AND. XOC.LT.XSREF2) THEN` | PPAR refinement limits XT/XB (XSREF/XPREF /= 1) |
| 1794 | 2 (fallthrough) | 0, 1216, 0 | `IF(XOC.GT.XSREF1 .AND. XOC.LT.XSREF2) THEN` | PPAR refinement limits XT/XB (XSREF/XPREF /= 1) |
| 1794 | 3 (jump) | 0, 1216, 0 | `IF(XOC.GT.XSREF1 .AND. XOC.LT.XSREF2) THEN` | PPAR refinement limits XT/XB (XSREF/XPREF /= 1) |
| 1802 | 0 (fallthrough) | 1214, 0, 0 | `IF(XOC.GT.XPREF1 .AND. XOC.LT.XPREF2) THEN` | PPAR refinement limits XT/XB (XSREF/XPREF /= 1) |
| 1802 | 2 (fallthrough) | 0, 1214, 0 | `IF(XOC.GT.XPREF1 .AND. XOC.LT.XPREF2) THEN` | PPAR refinement limits XT/XB (XSREF/XPREF /= 1) |
| 1802 | 3 (jump) | 0, 1214, 0 | `IF(XOC.GT.XPREF1 .AND. XOC.LT.XPREF2) THEN` | PPAR refinement limits XT/XB (XSREF/XPREF /= 1) |
| 1812 | 1 (jump) | 10 | `IF(IBLE.EQ.0) THEN` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0) |
| 1844 | 1 (jump) | 10 | `IF(IBLE.EQ.0) THEN` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0) |
| 1874 | 1 (jump) | 52 | `DO 10 ITER=1, 20` | iteration cap: PANGEN's 20-iteration node-placement Newton exhausted (DMAX never below 1e-3) |
| 1883 | 1 (jump) | 52 | `IF(CAVM .EQ. 0.0) THEN` | zero curvature at two consecutive nodes: a flat-sided section (flat plate, diamond) |
| 1898 | 1 (jump) | 37338 | `IF(CAVP .EQ. 0.0) THEN` | zero curvature at two consecutive nodes: a flat-sided section (flat plate, diamond) |
| 1955 | 0 (fallthrough) | 52 | `IF(IBLE.NE.0) THEN` | a buffer airfoil with a doubled LE node (sharp LE, IBLE /= 0): LOAD of such a file, then PPAR |
| 1973 | 0 (fallthrough) | 37390 | `IF(DSRAT.GT.4.0) RLX = (4.0-1.0)*DS/DDS` | a node-position Newton step that would stretch a panel by more than 4x (hard buffer geometry) |
| 2003 | 1 (fallthrough) | 2440 | `IF(SB(IB) .EQ. SB(IB+1)) THEN` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2015 | 0 (fallthrough) | 0 | `IF(S(I) .LE. SBCORN) GO TO 252` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2015 | 1 (jump) | 0 | `IF(S(I) .LE. SBCORN) GO TO 252` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2033 | 0 (fallthrough) | 0 | `IF(I-2 .GE. 1) THEN` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2033 | 1 (jump) | 0 | `IF(I-2 .GE. 1) THEN` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2039 | 0 (jump) | 0 | `IF(I+2 .LE. N) THEN` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2039 | 1 (fallthrough) | 0 | `IF(I+2 .LE. N) THEN` | a buffer airfoil with a corner (doubled node): the corner-insertion loop |
| 2106 | 0 (fallthrough) | 10 | `IF(SHARP) THEN` | PANGEN on a sharp-TE buffer airfoil (LOAD of a sharp section, then PPAR) |

### `xutils.f` — SETEXP

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 38 | 0 (fallthrough) | 105 | `IF(RATIO.EQ.1.0) GO TO 11` | the quadratic initial guess landing on RATIO = 1.0 bitwise |
| 41 | 1 (jump) | 315 | `DO 1 ITER=1, 100` | iteration cap: SETEXP's 100-iteration ratio Newton exhausted |

### `spline.f` — SPLIND

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 127 | 0 (fallthrough) | 270, 0, 0, 0, 0 | `IF(N.EQ.2 .AND. XS1.EQ.-999.0 .AND. XS2.EQ.-999.0) THEN` | a two-point spline segment (two adjacent corners in the buffer) |
| 127 | 2 (fallthrough) | 0, 270, 0, 0, 0 | `IF(N.EQ.2 .AND. XS1.EQ.-999.0 .AND. XS2.EQ.-999.0) THEN` | a two-point spline segment (two adjacent corners in the buffer) |
| 127 | 3 (jump) | 0, 270, 0, 0, 0 | `IF(N.EQ.2 .AND. XS1.EQ.-999.0 .AND. XS2.EQ.-999.0) THEN` | a two-point spline segment (two adjacent corners in the buffer) |
| 127 | 4 (fallthrough) | 0, 270, 0, 0, 0 | `IF(N.EQ.2 .AND. XS1.EQ.-999.0 .AND. XS2.EQ.-999.0) THEN` | a two-point spline segment (two adjacent corners in the buffer) |
| 127 | 5 (jump) | 0, 270, 0, 0, 0 | `IF(N.EQ.2 .AND. XS1.EQ.-999.0 .AND. XS2.EQ.-999.0) THEN` | a two-point spline segment (two adjacent corners in the buffer) |

### `spline.f` — SINVRT

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 421 | 1 (jump) | 88 | `DO 10 ITER=1, 10` | iteration cap: SINVRT's 10-iteration inversion exhausted (XTR trip far from any node on a coarse panelling) |

### `spline.f` — SEGSPL

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 547 | 1 (fallthrough) | 22342 | `IF(S(ISEG).EQ.S(ISEG+1)) THEN` | a buffer airfoil with a corner (doubled node) |

### `xgeom.f` — LEFIND

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 54 | 1 (jump) | 81 | `IF(S(I) .EQ. S(I-1)) THEN` | a buffer airfoil with a doubled LE node (sharp LE) |
| 60 | 1 (jump) | 144 | `DO 20 ITER=1, 50` | iteration cap: LEFIND's 50-iteration LE Newton exhausted |

### `xgeom.f` — GEOPAR

| line | edge | other edges | source | reach |
|---:|---|---|---|---|
| 350 | 1 (jump) | 22 | `IF(ABS(CURVLE) .GT. 0.001*(S(N)-S(1))) RADLE = 1.0 / CURVLE` | LE curvature below 0.001/(S(N)-S(1)): a flat plate or wedge (RADLE left 0) |

## Annotated unreachable branches

### `xpanel.f` — APCALC

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 29 | 0 (fallthrough) | 2635 | `IF(SX.EQ.0.0 .AND. SY.EQ.0.0) THEN` | *structural* — SX = SY = 0 needs two coincident consecutive nodes: ABCOPY deletes consecutive duplicates on LOAD and PANGEN inserts a buffer corner as one node |

### `xpanel.f` — NCALC

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 58 | 1 (jump) | 27 | `IF(N.LE.1) RETURN` | *structural* — N <= 1: every caller passes the paneled airfoil or the wake (N >= 3) |
| 66 | 0 (fallthrough) | 2662 | `IF(SMOD .EQ. 0.0) THEN` | *structural* — SMOD = 0 needs both spline derivatives zero at a node (a cusp), not producible from distinct ordered nodes |
| 77 | 1 (fallthrough) | 2635 | `IF(S(I) .EQ. S(I+1)) THEN` | *structural* — S(I) = S(I+1): coincident consecutive nodes (see line 29) |
| 81 | 0 (fallthrough) | 0 | `IF(SMOD .EQ. 0.0) THEN` | *structural* — S(I) = S(I+1): coincident consecutive nodes (see line 29) |
| 81 | 1 (jump) | 0 | `IF(SMOD .EQ. 0.0) THEN` | *structural* — S(I) = S(I+1): coincident consecutive nodes (see line 29) |

### `xpanel.f` — PSILIN

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 180 | 0 (jump) | 1312562 | `IF(DSO .EQ. 0.0) GO TO 10` | *structural* — DSO = 0 is a null panel: coincident consecutive nodes (see line 29) |
| 231 | 0 (fallthrough) | 1312562 | `IF(GEOLIN) THEN` | *mode* — GEOLIN = .TRUE. only from the inverse-design routines (xqdes.f:1340); every analysis-path caller passes .FALSE. |
| 386 | 1 (fallthrough) | 1302942 | `IF(GEOLIN) THEN` | *mode* — GEOLIN = .TRUE. only from the inverse-design routines (xqdes.f:1340); every analysis-path caller passes .FALSE. |
| 449 | 0 (fallthrough) | 9620 | `IF(GEOLIN) THEN` | *mode* — GEOLIN = .TRUE. only from the inverse-design routines (xqdes.f:1340); every analysis-path caller passes .FALSE. |
| 488 | 0 (jump) | 9738 | `IF(.NOT.LIMAGE) RETURN` | *mode* — LIMAGE is never .TRUE. in 6.99: its only toggle is commented out (xoper.f:405), so the image-airfoil block that follows is dead |
| 498 | 0 (fallthrough) | 0 | `IF(JO.EQ.1) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 498 | 1 (jump) | 0 | `IF(JO.EQ.1) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 500 | 0 (fallthrough) | 0 | `ELSE IF(JO.EQ.N-1) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 500 | 1 (jump) | 0 | `ELSE IF(JO.EQ.N-1) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 502 | 0 (fallthrough) | 0 | `ELSE IF(JO.EQ.N) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 502 | 1 (jump) | 0 | `ELSE IF(JO.EQ.N) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 504 | 0 (fallthrough) | 0 | `IF((X(JO)-X(JP))**2 + (Y(JO)-Y(JP))**2 .LT. SEPS**2) GO TO 22` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 504 | 1 (jump) | 0 | `IF((X(JO)-X(JP))**2 + (Y(JO)-Y(JP))**2 .LT. SEPS**2) GO TO 22` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 510 | 0 (jump) | 0 | `IF(DSO .EQ. 0.0) GO TO 20` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 510 | 1 (fallthrough) | 0 | `IF(DSO .EQ. 0.0) GO TO 20` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 538 | 0 (fallthrough) | 0, 0, 0 | `IF(IO.GE.1 .AND. IO.LE.N) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 538 | 1 (jump) | 0, 0, 0 | `IF(IO.GE.1 .AND. IO.LE.N) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 538 | 2 (fallthrough) | 0, 0, 0 | `IF(IO.GE.1 .AND. IO.LE.N) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 538 | 3 (jump) | 0, 0, 0 | `IF(IO.GE.1 .AND. IO.LE.N) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 557 | 0 (fallthrough) | 0 | `IF(GEOLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 557 | 1 (jump) | 0 | `IF(GEOLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 571 | 0 (fallthrough) | 0 | `IF(JO.EQ.N) GO TO 21` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 571 | 1 (jump) | 0 | `IF(JO.EQ.N) GO TO 21` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 573 | 0 (fallthrough) | 0 | `IF(SIGLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 573 | 1 (jump) | 0 | `IF(SIGLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 712 | 0 (jump) | 0 | `IF(GEOLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 712 | 1 (fallthrough) | 0 | `IF(GEOLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 775 | 0 (fallthrough) | 0 | `IF(GEOLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |
| 775 | 1 (jump) | 0 | `IF(GEOLIN) THEN` | *mode* — inside the LIMAGE image-airfoil block (see line 488) |

### `xpanel.f` — PSWLIN

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 861 | 2 (fallthrough) | 45138, 297240, 45138 | `IF(IO.GE.N+1 .AND. IO.LE.N+NW) THEN` | *structural* — IO > N+NW is impossible: PSWLIN's only callers (QDCALC, xpanel.f:1178 and :1227) pass IO <= N+NW |
| 867 | 3 (jump) | 340284, 2094, 340284 | `IF(IO.NE.JO .AND. RS1.GT.0.0) THEN` | *structural* — RS1/RS2 = 0 with IO /= JO needs a node coincident with another node; wake nodes are offset from the TE and strictly downstream |
| 875 | 3 (jump) | 340284, 2094, 340284 | `IF(IO.NE.JP .AND. RS2.GT.0.0) THEN` | *structural* — RS1/RS2 = 0 with IO /= JO needs a node coincident with another node; wake nodes are offset from the TE and strictly downstream |

### `xpanel.f` — XYWAKE

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 1281 | 0 (fallthrough) | 105 | `IF(NW.GT.IWX) THEN` | *guard* — NW > IWX array-bound STOP; NW = N/12 + 10*WAKLEN is within IWX for every case dimension (yFoil has no fixed dimensions) |

### `xpanel.f` — IBLPAN

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 1444 | 0 (fallthrough) | 267 | `IF(IBLMAX.GT.IVX) THEN` | *guard* — IBLMAX > IVX array-bound STOP (see line 1281) |

### `xbl.f` — SETBL

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 488 | 3 (jump) | 2448, 202536, 2448 | `IF(IBL .EQ. ITRAN(IS) .AND. X2 .GT. X1) THEN` | *structural* — X2 > X1 always: XSSI is strictly increasing (XICALC with the XEPS floor) |

### `xbl.f` — IBLSYS

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 536 | 0 (fallthrough) | 267 | `IF(NSYS.GT.2*IVX) STOP '*** IBLSYS: BL system array overflow. ***'` | *guard* — NSYS > 2*IVX array-bound STOP (yFoil has no fixed dimensions) |

### `xoper.f` — SPECAL

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 2740 | 2 (fallthrough) | 89, 16, 89 | `IF(.NOT.LGAMU .OR. .NOT.LQAIJ) CALL GGCALC` | *structural* — LQAIJ is never cleared without LGAMU (INIT, PANGEN, xpol, xgdes, xqdes clear both), so .NOT.LQAIJ is never evaluated true once LGAMU holds |
| 2784 | 5 (jump) | 206, 7, 2, 5, 5 | `IF(MATYP.EQ.1 .OR. MINF.EQ.0.0 .OR. MINF_CLM.NE.0.0) GO TO 91` | *structural* — MINF_CLM = 0 with MATYP /= 1 and MINF /= 0 needs MATYP = 3, which the TYPE command never sets; for MATYP = 2, MINF_CLM = -0.5*MINF/CL is never 0 |
| 2813 | 0 (fallthrough) | 105 | `IF(LFLAP) CALL MHINGE` | *mode* — LFLAP: flap hinge moments (MHINGE) are out of scope |

### `xoper.f` — SPECCL

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 2826 | 2 (fallthrough) | 1, 1, 1 | `IF(.NOT.LGAMU .OR. .NOT.LQAIJ) CALL GGCALC` | *structural* — LQAIJ is never cleared without LGAMU (see line 2740) |
| 2880 | 0 (fallthrough) | 2 | `IF(LFLAP) CALL MHINGE` | *mode* — LFLAP: flap hinge moments (MHINGE) are out of scope |

### `xoper.f` — VISCAL

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 2910 | 0 (fallthrough) | 19 | `IF(LBLINI) CALL GAMQV` | *mode* — LIPAN cleared while LBLINI stays only happens in the QDES design routine (xqdes.f:497, LBLINI deliberately left); INIT, WAKL, PANGEN and GDES clear both |
| 2958 | 2 (fallthrough) | 2, 105, 2 | `IF(.NOT.LWDIJ .OR. .NOT.LADIJ) CALL QDCALC` | *structural* — LADIJ false while LWDIJ is true is impossible: LADIJ is only cleared together with LWDIJ (INIT, PANGEN, design routines) and XYWAKE clears LWDIJ alone |
| 2961 | 0 (fallthrough) | 107 | `IF(NITER.EQ.0) CALL ASKI('Enter number of iterations^',NITER)` | *mode* — NITER = 0 prompts interactively (ASKI); ITER is pinned in every run |
| 3022 | 0 (fallthrough) | 107 | `IF(LFLAP) CALL MHINGE` | *mode* — LFLAP: flap hinge moments (MHINGE) are out of scope |

### `xsolve.f` — LUDCMP

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 193 | 1 (jump) | 17 | `IF(N.GT.NVX) STOP 'LUDCMP: Array overflow. Increase NVX.'` | *guard* — N > NVX array-bound STOP (yFoil has no fixed dimensions) |

### `xfoil.f` — MRCL

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 795 | 0 (fallthrough) | 2903 | `IF(RETYP.LT.1 .OR. RETYP.GT.3) THEN` | *guard* — illegal RETYP/MATYP: only 1..3 are producible through the TYPE command; yFoil's type is an enum |
| 800 | 0 (fallthrough) | 2903 | `IF(MATYP.LT.1 .OR. MATYP.GT.3) THEN` | *guard* — illegal RETYP/MATYP: only 1..3 are producible through the TYPE command; yFoil's type is an enum |
| 812 | 1 (jump) | 35 | `ELSE IF(MATYP.EQ.2) THEN` | *structural* — MATYP = 3 is never set: the TYPE command maps TYPE 3 to MATYP = 1, RETYP = 3 (xoper.f:362) |
| 817 | 0 (fallthrough) | 0 | `ELSE IF(MATYP.EQ.3) THEN` | *structural* — MATYP = 3 is never set (see line 812) |
| 817 | 1 (jump) | 0 | `ELSE IF(MATYP.EQ.3) THEN` | *structural* — MATYP = 3 is never set (see line 812) |
| 835 | 1 (jump) | 23 | `ELSE IF(RETYP.EQ.3) THEN` | *guard* — RETYP outside 1..3 when the RETYP = 3 test is reached: only 1..3 are producible through TYPE |

### `xfoil.f` — NACA

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 1574 | 0 (fallthrough) | 5 | `IF(IDES1 .LE. 0) THEN` | *mode* — NACA with no designation prompts interactively (ASKI) |
| 1581 | 1 (jump) | 5 | `IF(IDES.LE.25099) ITYPE = 5` | *guard* — designation > 25099 is rejected; yFoil returns NacaError before PANGEN |
| 1584 | 0 (fallthrough) | 1 | `IF(ITYPE.EQ.0) THEN` | *guard* — unrecognised designation; yFoil returns NacaError before PANGEN |
| 1594 | 1 (jump) | 5 | `IF(IDES.EQ.0) RETURN` | *guard* — unrecognised designation; yFoil returns NacaError before PANGEN |

### `xfoil.f` — PANGEN

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 1636 | 0 (fallthrough) | 10 | `IF(NB.LT.2) THEN` | *guard* — NB < 2: no buffer airfoil; yFoil's generator always supplies one |
| 2025 | 0 (fallthrough) | 0 | `IF(N .GT. IQX-1)` | *guard* — N > IQX-1 array-bound STOP (yFoil has no fixed dimensions) |
| 2025 | 1 (jump) | 0 | `IF(N .GT. IQX-1)` | *guard* — N > IQX-1 array-bound STOP (yFoil has no fixed dimensions) |
| 2091 | 0 (fallthrough) | 10 | `IF(LBFLAP) THEN` | *mode* — LBFLAP: flap deflection is out of scope |

### `xutils.f` — SETEXP

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 30 | 0 (fallthrough) | 105 | `IF(NEX.LE.1) THEN` | *guard* — NEX <= 1 STOP: impossible with NW >= 10 |
| 32 | 0 (fallthrough) | 105 | `ELSE IF(NEX.EQ.2) THEN` | *structural* — NEX = 2 impossible: SETEXP's only analysis-path caller is XYWAKE with NW = N/12 + 10*WAKLEN >= 10 |

### `naca.f` — NACA4

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 75 | 1 (jump) | 4, 4, 0 | `NAME = 'NACA'` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 75 | 3 (jump) | 4, 0, 4 | `NAME = 'NACA'` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |

### `naca.f` — NACA5

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 171 | 1 (jump) | 1, 1, 0 | `NAME = 'NACA'` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 171 | 3 (jump) | 1, 0, 1 | `NAME = 'NACA'` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 179 | 0 (jump) | 1, 0, 0, 0, 0 | `END` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 179 | 2 (jump) | 0, 1, 0, 0, 0 | `END` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 179 | 3 (jump) | 0, 1, 0, 0, 0 | `END` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 179 | 4 (jump) | 0, 1, 0, 0, 0 | `END` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |
| 179 | 5 (jump) | 0, 1, 0, 0, 0 | `END` | *compiler* — edges generated for CHARACTER concatenation length handling; no source-level decision |

### `spline.f` — SPLIND

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 85 | 1 (jump) | 270 | `IF(N.GT.NMAX) STOP 'SPLIND: array overflow, increase NMAX'` | *guard* — N > NMAX array-bound STOP (yFoil has no fixed dimensions) |
| 96 | 0 (fallthrough) | 270 | `IF(XS1.EQ.999.0) THEN` | *structural* — SPLIND is reached only through SEGSPL (end conditions -999/-999) on the analysis path; the 999 (zero second derivative) and specified-slope conditions belong to SPLINE (plotting) and the design routines |
| 101 | 1 (jump) | 270 | `ELSE IF(XS1.EQ.-999.0) THEN` | *structural* — see line 96 |
| 113 | 0 (fallthrough) | 270 | `IF(XS2.EQ.999.0) THEN` | *structural* — SPLIND is reached only through SEGSPL (end conditions -999/-999) on the analysis path; the 999 (zero second derivative) and specified-slope conditions belong to SPLINE (plotting) and the design routines |
| 117 | 1 (jump) | 270 | `ELSE IF(XS2.EQ.-999.0) THEN` | *structural* — see line 96 |

### `spline.f` — SEGSPL

| line | edge | other edges | source | why |
|---:|---|---|---|---|
| 542 | 0 (fallthrough) | 182 | `IF(S(1).EQ.S(2)  ) STOP 'SEGSPL:  First input point duplicated'` | *guard* — duplicated end point STOP: ABCOPY/PANGEN never produce one |
| 543 | 0 (fallthrough) | 182 | `IF(S(N).EQ.S(N-1)) STOP 'SEGSPL:  Last  input point duplicated'` | *guard* — duplicated end point STOP: ABCOPY/PANGEN never produce one |

## DO-loop zero-trip entry edges never taken

### `xpanel.f` — PSILIN

| line | edge | other edges | source |
|---:|---|---|---|
| 162 | 1 (jump) | 1312680 | `DO 10 JO=1, N` |
| 492 | 0 (fallthrough) | 0 | `DO 20 JO=1, N` |
| 492 | 1 (jump) | 0 | `DO 20 JO=1, N` |

### `xpanel.f` — STFIND

| line | edge | other edges | source |
|---:|---|---|---|
| 1364 | 1 (jump) | 90670 | `DO 10 I=1, N-1` |

### `xpanel.f` — UECALC

| line | edge | other edges | source |
|---:|---|---|---|
| 1568 | 0 (fallthrough) | 0 | `DO 10 IS=1, 2` |
| 1568 | 1 (jump) | 0 | `DO 10 IS=1, 2` |
| 1570 | 0 (fallthrough) | 0 | `DO 110 IBL=2, NBL(IS)` |
| 1570 | 1 (jump) | 0 | `DO 110 IBL=2, NBL(IS)` |

### `xpanel.f` — DSSET

| line | edge | other edges | source |
|---:|---|---|---|
| 1789 | 0 (fallthrough) | 0 | `DO 1 IS=1, 2` |
| 1789 | 1 (jump) | 0 | `DO 1 IS=1, 2` |
| 1790 | 0 (fallthrough) | 0 | `DO 10 IBL=2, NBL(IS)` |
| 1790 | 1 (jump) | 0 | `DO 10 IBL=2, NBL(IS)` |

### `xfoil.f` — PANGEN

| line | edge | other edges | source |
|---:|---|---|---|
| 1860 | 0 (fallthrough) | 0 | `DO I=2, NN1` |
| 1860 | 1 (jump) | 0 | `DO I=2, NN1` |
| 1866 | 0 (fallthrough) | 0 | `DO I=2, NN2-1` |
| 1866 | 1 (jump) | 0 | `DO I=2, NN2-1` |
| 2012 | 0 (jump) | 0 | `DO 252 I=1, N` |
| 2012 | 1 (fallthrough) | 0 | `DO 252 I=1, N` |
| 2018 | 0 (fallthrough) | 0 | `DO 2522 J=N, I, -1` |
| 2018 | 1 (jump) | 0 | `DO 2522 J=N, I, -1` |

### `xgeom.f` — LEFIND

| line | edge | other edges | source |
|---:|---|---|---|
| 42 | 0 (fallthrough) | 4864 | `DO 10 I=3, N-2` |


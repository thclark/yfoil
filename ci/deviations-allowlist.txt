# Deliberate, temporary deviations from XFOIL that CI's no-deviations gate tolerates.
# Format: <file>:<pattern>   # <stage that removes it>
# Each line here is a debt; the plan stage named removes it and the line is deleted.
src/solver/setbl_legacy.rs:disable_vm            # S9: legacy path deleted with the VISCAL rewrite
src/solver/setbl_legacy.rs:disable_dij_coupling  # S9: legacy path deleted with the VISCAL rewrite
src/solver/setbl_legacy.rs:tighter than XFOIL    # S9: legacy path deleted with the VISCAL rewrite
src/solver/setbl_legacy.rs:TEMPORARILY DISABLED    # S9: legacy path deleted with the VISCAL rewrite
src/solver/setbl_legacy.rs:coupling_scale          # S9: legacy path deleted with the VISCAL rewrite
src/solver/viscal.rs:causes divergence      # S9: legacy path deleted with the VISCAL rewrite
src/solver/setbl_legacy.rs:for stability           # S9: legacy path deleted with the VISCAL rewrite
src/bl/march_legacy.rs:for stability        # S9: legacy path deleted with the VISCAL rewrite
